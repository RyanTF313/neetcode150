# Valid Parentheses

### Level
Easy

### Description
Given a string `s` containing only the characters `(`, `)`, `{`, `}`, `[` and `]`, return `true` if the input string is valid, otherwise return `false`.

A string is valid if open brackets are closed by the same type of brackets and in the correct order.
